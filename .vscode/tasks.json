{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Windows Build",
            "command": "${workspaceFolder}\\build_vk.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            //"type": "shell",
        }
    ]
}