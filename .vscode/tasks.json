{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Windows Build",
            "command": "${workspaceFolder}/build.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}